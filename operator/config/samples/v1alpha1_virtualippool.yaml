apiVersion: metalk8s.scality.com/v1alpha1
kind: VirtualIPPool
metadata:
  labels:
    app.kubernetes.io/name: operator
    app.kubernetes.io/managed-by: kustomize
  name: virtualippool-sample
spec:
  # TODO(user): Add fields here
