# Kubelet standalone service configuration file generated by MetalK8s
[Service]
Environment="KUBELET_CONFIG_ARGS=--pod-manifest-path={{ manifest_path }}"
EnvironmentFile=-{{ env_file }}
# This is a file that the user can use for overrides of the kubelet args as a last resort. Preferably, the user should use
# the .NodeRegistration.KubeletExtraArgs object in the configuration files instead. KUBELET_EXTRA_ARGS should be sourced from this file.
EnvironmentFile=-/etc/sysconfig/kubelet
ExecStart=
ExecStart=/usr/bin/kubelet $KUBELET_CONFIG_ARGS $KUBELET_KUBEADM_ARGS $KUBELET_EXTRA_ARGS
