/** Translates the old colors into new colors while keeping the same name.
 * New names are also supported. */
export declare const getThemePropSelector: (key: any) => (props: any) => any;
/** Translates the old colors into new colors while keeping same name.
 * New names are also supported. */
export declare const getThemeVariantSelector: () => (props: any) => any;
export declare const hex2RGB: (str: string) => [number, number, number];
export declare const convertRemToPixels: (rem: number) => number;
//# sourceMappingURL=utils.d.ts.map