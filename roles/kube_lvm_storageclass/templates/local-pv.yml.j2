apiVersion: v1
kind: PersistentVolume
metadata:
  name: >-
    {{ item.value.vg_name|replace("_","-") }}-{{ item.value.uuid }}
spec:
  capacity:
    storage: '{{ item.value.size|size_lvm_to_k8s }}'
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: '{{ item.value.storageclass }}'
  local:
    path: '{{ item.value.host_path }}/{{ item.value.uuid }}'
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - '{{ ansible_nodename }}'
