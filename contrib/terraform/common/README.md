It feels like terraform 'module' are completely broken
(you can't change variable of child module, except by redefining it in the parent and passing to it,
but then you loose the default value of child)
