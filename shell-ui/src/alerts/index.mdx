import { useContext, createContext } from 'react';
import {useQuery, QueryClientProvider, QueryClient} from 'react-query';

export const TestAlert = () => {
    const alerts = useAlerts({alertname: 'KubeCPUOvercommit'});
    return <>{JSON.stringify(alerts)}</>
}

export const AlertProvider = alertLibrary.AlertProvider(useQuery);

<QueryClientProvider client={new QueryClient()}>
    <AlertProvider alertManagerUrl='https://10.200.6.201:8443/api/alertmanager'>
        <TestAlert />
    </AlertProvider>
</QueryClientProvider>
