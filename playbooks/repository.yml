---
- name: Set up package repositories
  hosts: metalk8s-repository
  tags:
  - repository
  roles:
    - role: repository
      tags: ['repository']
    - role: repository/populate
      tags: ['repository', 'populate']
